(()=>{const status=document.getElementById('newsletterConfirmStatus'),button=document.getElementById('newsletterConfirmButton'),token=new URLSearchParams(location.search).get('token')||'';if(!token||!token.startsWith('c_')){status.textContent='This confirmation link is invalid or has expired.';button.disabled=true;return}button.onclick=async()=>{button.disabled=true;button.textContent='Confirming…';try{const r=await fetch('/api/newsletter-confirm',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})}),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'We could not confirm your subscription.');status.textContent=d.message||'Your Namdar newsletter subscription is confirmed.';button.textContent='Confirmed ✓'}catch(e){status.textContent=e.message||'We could not confirm your subscription.';button.disabled=false;button.textContent='Try again'}}})();
