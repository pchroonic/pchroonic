const {json,db,safeError,safeHttpsUrl}=require('../lib/server');
const labels={windows:'Window cleaning',gutters:'Gutter cleaning',roof:'Roof cleaning',jetwash:'Jet washing',handyman:'Handyman',tour3d:'3D property tour'};
module.exports=async function(req,res){try{if(req.method!=='GET')return json(res,405,{ok:false,error:'Method not allowed'});const url=new URL(req.url,'https://namdar.co.uk');const slug=String(url.searchParams.get('slug')||'').slice(0,80);if(!slug)return json(res,400,{ok:false,error:'Project is required.'});const rows=await db(`customer_projects?share_slug=eq.${encodeURIComponent(slug)}&public_shared=eq.true&select=title,service_key,before_images,after_images,tour_url,final_price,show_price,customer_comment,customer_rating&limit=1`);const p=rows?.[0];if(!p)return json(res,404,{ok:false,error:'This project is not public.'});return json(res,200,{ok:true,project:{title:p.title,serviceLabel:labels[p.service_key]||p.service_key,beforeUrls:(p.before_images||[]).map(safeHttpsUrl).filter(Boolean),afterUrls:(p.after_images||[]).map(safeHttpsUrl).filter(Boolean),tourUrl:safeHttpsUrl(p.tour_url||'')||null,finalPrice:p.show_price?p.final_price:null,customerComment:p.customer_comment||null,customerRating:p.customer_rating||null}})}catch(e){return safeError(res,e)}};
