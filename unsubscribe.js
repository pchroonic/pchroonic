const el=document.getElementById('unsubscribeStatus');fetch('/api/newsletter-unsubscribe?token='+encodeURIComponent(new URLSearchParams(location.search).get('token')||''),{method:'POST'}).then(r=>r.json()).then(d=>el.textContent=d.ok?'You have been unsubscribed from Namdar marketing emails. Essential service emails will still be sent when needed.':(d.error||'We could not update your preference.')).catch(()=>el.textContent='We could not update your preference.');
