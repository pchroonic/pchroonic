const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));fetch('/api/public-data').then(r=>r.json()).then(d=>{const el=document.querySelector('#publicWorkList'),jobs=d.jobs||[];if(!jobs.length){el.innerHTML='<div class="seo-empty"><strong>No public case studies yet.</strong><p>Namdar only shows jobs here after the team has deliberately published them.</p></div>';return}el.innerHTML=jobs.map(j=>{const image=Array.isArray(j.image_urls)&&j.image_urls[0]?`<img src="${esc(j.image_urls[0])}" alt="${esc(j.title)}" loading="lazy">`:'';return `<article class="job-card">${image}<div class="job-body"><small>${esc(j.location_label||'')}</small><h2><a href="/work/${encodeURIComponent(j.id)}">${esc(j.title)}</a></h2><p>${esc(j.description||'Completed by Namdar.')}</p><a class="text-link" href="/work/${encodeURIComponent(j.id)}">View project →</a></div></article>`}).join('')}).catch(()=>{document.querySelector('#publicWorkList').innerHTML='<p>Published work is temporarily unavailable.</p>'});