const out=document.getElementById('recoverResult'),btn=document.getElementById('recoverAccount');
btn.onclick=async()=>{const token=new URLSearchParams(location.search).get('token');if(!token){out.textContent='Invalid recovery link.';return}btn.disabled=true;out.textContent='Recovering…';try{const r=await fetch('/api/account-recover',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})}),d=await r.json();if(!r.ok)throw new Error(d.error||'Could not recover account');out.textContent='Your account is active again. You can now sign in to My Namdar.'}catch(e){out.textContent=e.message}finally{btn.disabled=false}};
