const out=document.getElementById('deleteResult'),btn=document.getElementById('confirmDelete');
btn.onclick=async()=>{const token=new URLSearchParams(location.search).get('token');if(!token){out.textContent='Invalid deletion link.';return}btn.disabled=true;out.textContent='Confirming…';try{const r=await fetch('/api/account-delete-confirm',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})}),d=await r.json();if(!r.ok)throw new Error(d.error||'Could not confirm deletion');out.textContent=`Account deactivated. Recovery is available until ${new Date(d.deleteAfter).toLocaleDateString('en-GB')}. Check your email for the recovery link.`}catch(e){out.textContent=e.message}finally{btn.disabled=false}};
